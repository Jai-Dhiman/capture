{
  "name": "server",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "wrangler d1 migrations apply capture-db"
  },
  "dependencies": {
    "@capture/validation": "workspace:*",
    "better-auth": "^1.1.14",
    "drizzle-orm": "^0.38.4",
    "hono": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "drizzle-kit": "^0.30.2",
    "wrangler": "^3.0.0"
  }
}